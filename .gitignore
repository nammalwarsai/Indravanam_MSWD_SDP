# Ignore node_modules for client and server only
client/node_modules/
server/node_modules/

# Do not ignore the main node_modules (for TailwindCSS and other dependencies)
# Keep this to avoid redeploy issues if these are needed
!/node_modules

# Ignore build directories
build/
client/build/
server/build/

# Ignore environment files (sensitive data)
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
server/.env

# Ignore package-lock and yarn.lock files if not essential
# You can remove these from ignore if you want exact version tracking for dependencies
package-lock.json
yarn.lock
client/yarn.lock
server/yarn.lock

# Ignore logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore coverage directories (if using testing tools)
coverage/
client/coverage/
server/coverage/

# Ignore system files
.DS_Store
Thumbs.db

# Ignore miscellaneous generated files
*.log
*.tmp
*.bak
